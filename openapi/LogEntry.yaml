components:
  schemas:
    LogEntry:
      anyOf:
      - $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/idRef
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_0_11.yaml#/components/schemas/LogEntry
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_1_9.yaml#/components/schemas/LogEntry
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_2_7.yaml#/components/schemas/LogEntry
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_3_6.yaml#/components/schemas/LogEntry
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_4_5.yaml#/components/schemas/LogEntry
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_5_1.yaml#/components/schemas/LogEntry
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_6_0.yaml#/components/schemas/LogEntry
      description: The LogEntry schema defines the record format for a log.  It is
        designed for Redfish event logs, OEM-specific log formats, and the IPMI System
        Event Log (SEL).  The EntryType field indicates the type of log and the Resource
        includes several additional properties dependent on the EntryType.
      x-longDescription: This Resource shall represent the log format for Log Services
        in a Redfish implementation.
title: '#LogEntry.LogEntry'
x-copyright: Copyright 2014-2020 DMTF. For the full DMTF copyright policy, see http://www.dmtf.org/about/policies/copyright
x-owningEntity: DMTF
