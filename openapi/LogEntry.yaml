components:
  schemas:
    LogEntry:
      anyOf:
      - $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/idRef
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_0_12.yaml#/components/schemas/LogEntry
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_1_10.yaml#/components/schemas/LogEntry
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_2_8.yaml#/components/schemas/LogEntry
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_3_7.yaml#/components/schemas/LogEntry
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_4_6.yaml#/components/schemas/LogEntry
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_5_2.yaml#/components/schemas/LogEntry
      - $ref: http://redfish.dmtf.org/schemas/v1/LogEntry.v1_6_1.yaml#/components/schemas/LogEntry
      description: The LogEntry schema defines the record format for a log.  It is
        designed for Redfish event logs, OEM-specific log formats, and the IPMI System
        Event Log (SEL).  The EntryType field indicates the type of log and the resource
        includes several additional properties dependent on the EntryType.
      x-longDescription: This resource shall represent the log format for log services
        in a Redfish implementation.
title: '#LogEntry.LogEntry'
x-copyright: Copyright 2014-2020 DMTF. For the full DMTF copyright policy, see http://www.dmtf.org/about/policies/copyright
x-owningEntity: DMTF
