components:
  schemas:
    SecurityPolicy_SecurityPolicy:
      anyOf:
      - $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_idRef
      - $ref: http://redfish.dmtf.org/schemas/v1/SecurityPolicy.v1_0_0.yaml#/components/schemas/SecurityPolicy_v1_0_0_SecurityPolicy
      description: The SecurityPolicy resource provides a central point to configure
        the security policy of a manager.
      x-longDescription: This resource shall represent configurable security related
        policies managed by a manager.  All security parameters in other resources
        that are controlled by the manager shall follow to the related settings in
        this security policy.  For example, an outbound TLS connection established
        per an EventDestination resource will follow the values of the properties
        in the TLS property.
title: '#SecurityPolicy.SecurityPolicy'
x-copyright: Copyright 2014-2022 DMTF. For the full DMTF copyright policy, see http://www.dmtf.org/about/policies/copyright
x-owningEntity: DMTF
